# Import topology + user on boot (see definitions.json; password is voltnet).
# RABBITMQ_DEFAULT_USER/PASS in compose are ignored when definitions include users.
definitions.import_backend = local_filesystem
definitions.local.path = /etc/rabbitmq/definitions.json
