# Local development: expose the API on http://localhost:3000
#
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build

services:
  gateway:
    ports:
      - "${GATEWAY_PUBLISH_PORT:-3000}:3000"
