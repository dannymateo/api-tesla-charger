# billing-service

Servicio NestJS aislado con arquitectura hexagonal.

## Persistencia

Este servicio usa `Prisma ORM` + `Prisma Migrations`.

Comandos utiles:

- `npm run prisma:generate`
- `npm run prisma:migrate:dev`
- `npm run prisma:migrate:deploy`
